import React from 'react'

const BannerSection = () => {
    return (
        <div class="section no-pad-bot" id="index-banner">
            <div class="container">
                <h1 class="header center orange-text">Where do we start ?</h1>
                <div class="row center">
                    <h5 class="header col s12 light">Build your schedule in a reactive way!</h5>
                </div>
                <div class="row center">
                    <button id="download-button" class="btn-large waves-effect waves-light orange">Create List</button>
                </div>
            </div>
        </div>
    )
}

export default BannerSection